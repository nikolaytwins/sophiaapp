import { PersonalTargetsScreen } from '@/features/goals/PersonalTargetsScreen';

/** Персональная доска (Китай/подушка, боковые цели) — не путать с годовыми целями на /goals. */
export default function PersonalTargetsRoute() {
  return <PersonalTargetsScreen />;
}
